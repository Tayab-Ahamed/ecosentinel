# Full production install (includes OpenAI Whisper → PyTorch + DB + Auth). For CI or API-only, use requirements-base.txt.
-r requirements-base.txt
openai-whisper==20240930
sqlmodel==0.0.22
alembic==1.13.3
asyncpg==0.29.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
fastapi-users[sqlalchemy]==0.1.0
slowapi==0.1.9
python-multipart==0.0.20  # Ensure for file uploads

